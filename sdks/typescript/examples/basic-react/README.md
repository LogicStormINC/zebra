# basic-react example

Minimal integration sketch: mount one readable + one action through the
Host BFF proxy (see ADR-CLIENT-01). Requires a published frontend
capability profile for the `frontend_app_id`.
