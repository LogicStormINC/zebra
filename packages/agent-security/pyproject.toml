[project]
name = "agent-security"
version = "0.1.0"
description = "Policy and security package for Zebra Agent."
requires-python = ">=3.12,<4.0"
dependencies = [
    "agent-core",
    "agent-tools",
]

[tool.uv.sources]
agent-core = { workspace = true }
agent-tools = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
