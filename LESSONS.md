# Lessons

跨会话保留的经验教训。每条记录：发生了什么、正确的认知是什么、
以后如何避免。新条目加在顶部。

## 2026-08-18 · `cloud-agent` 分支是项目主线，不是遗留分支

**发生了什么**：2026-08-18 清理分支时，助手按"提交是否已包含进
`main`"判定可删性，把远程更名后的 `cloud-agent` 分支（原
`zebra-cloud-trench）`误判为可清理的遗留指针并提议删除。maintainer
纠正：`cloud-agent` 是他要求设立的本项目主开发线。

**正确认知**：

- 远程 `cloud-agent` 分支是**项目主线**（maintainer directive,
  2026-08-18），即使其提交内容已全部包含在 `main` 的历史里。
  `main` 继续作为 PR 目标与发布历史；主线的实现工作以 `cloud-agent`
  为基准展开。
- 任何会话都不得把 `cloud-agent` 当作"已合并旧分支"删除、归档或
  重命名。

**以后如何避免**：

- 判断一个分支去留时，"内容是否已被包含"只是必要信息，不是充分
  结论；长生命周期主线分支可以长期与 `main` 内容重叠。拿不准分支
  语义时，先问 maintainer，再动手。
- 对刚出现/刚更名的分支（尤其是 maintainer 当天操作的），默认视为
  有意为之，不纳入批量清理。
